//
// Created by Abderrahim on 12/25/2022.
//

#include "heuristics.h"

void heuristic1(task_t tab[NB_TACHE_MAX], unsigned int size) {
    quickSort(tab, 0, size - 1, withDueDate);


}


void heuristic2(task_t tab[NB_TACHE_MAX], unsigned int size) {
    quickSort(tab, 0, size - 1, withDueDate);

}


void heuristic3(task_t tab[NB_TACHE_MAX], unsigned int size) {
    quickSort(tab, 0, size - 1, withDueDate);

}


void heuristic4(task_t tab[NB_TACHE_MAX], unsigned int size) {
    quickSort(tab, 0, size - 1, withDueDate);

}

void localSearch(task_t tab[NB_TACHE_MAX], unsigned int size) {
    quickSort(tab, 0, size - 1, withDueDate);

}
